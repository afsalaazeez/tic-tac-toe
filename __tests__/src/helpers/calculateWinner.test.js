// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic-cli using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
import calculateWinner from "../../../src/helpers/calculateWinner";

describe("calculateWinner", () => {
  test("should return 'X' as the winner if 'X' occupies the first row", () => {
    const squares = ['X', 'X', 'X', null, null, null, null, null, null];
    expect(calculateWinner(squares)).toBe('X');
  });

  test("should return 'O' as the winner if 'O' occupies the second column", () => {
    const squares = [null, 'O', null, null, 'O', null, null, 'O', null];
    expect(calculateWinner(squares)).toBe('O');
  });

  test("should return null if there is no winner", () => {
    const squares = ['X', 'X', 'O', 'O', 'O', 'X', 'X', null, null];
    expect(calculateWinner(squares)).toBeNull();
  });

  test("should handle unexpected input types", () => {
    const squares = [1, 'X', 'X', 'O', true, null, 'O', null, 'O'];
    expect(calculateWinner(squares)).toBeNull(); // Assuming it gracefully handles unexpected types
  });
});

