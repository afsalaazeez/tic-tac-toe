// ********RoostGPT********
// Test generated by RoostGPT for test react-tic using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
// Inside the describe("Board component tests", () => { ... });

test('renders 9 squares and a restart button', () => {
  const { getAllByRole, getByText } = render(<Board {...props} />);
  expect(getAllByRole('button').length).toBe(9); // Each square is a button
  expect(getByText('Restart the game')).toBeInTheDocument();
});

test('calls onClick callback when a square is clicked', () => {
  const { getAllByRole } = render(<Board {...props} />);
  const squares = getAllByRole('button');
  fireEvent.click(squares[0]);
  expect(props.onClick).toHaveBeenCalledWith(0);
});

test('calls jumpTo callback when the restart button is clicked', () => {
  const { getByText } = render(<Board {...props} />);
  const restartButton = getByText('Restart the game');
  fireEvent.click(restartButton);
  expect(props.jumpTo).toHaveBeenCalledWith(0);
});

