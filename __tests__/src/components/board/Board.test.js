// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic-cli using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
import React from 'react';
import { render, cleanup, fireEvent } from '@testing-library/react';
import Board from '../../../../src/components/board/Board';

// Describe block for Board component
describe('Board Component', () => {
    afterEach(cleanup);

    test('renders the board with squares and a restart button', () => {
        const { getByText, getAllByRole } = render(
            <Board squares={Array(9).fill(null)} onClick={() => {}} jumpTo={() => {}} />
        );
        expect(getAllByRole('button').length).toBe(9); // Expecting 9 squares
        expect(getByText('Restart the game')).toBeInTheDocument(); // Check if the restart button is in the document
    });

    test('click on square calls onClick handler with index', () => {
        const onClick = jest.fn();
        const { getAllByRole } = render(
            <Board squares={Array(9).fill(null)} onClick={onClick} jumpTo={() => {}} />
        );
        fireEvent.click(getAllByRole('button')[0]); // Click the first square
        expect(onClick).toHaveBeenCalledWith(0); // Expect onClick to have been called with 0
    });

    test('click on restart button calls jumpTo with 0', () => {
        const jumpTo = jest.fn();
        const { getByText } = render(
            <Board squares={Array(9).fill(null)} onClick={() => {}} jumpTo={jumpTo} />
        );
        fireEvent.click(getByText('Restart the game'));
        expect(jumpTo).toHaveBeenCalledWith(0);
    });
});

