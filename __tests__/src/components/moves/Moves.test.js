// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic-cli using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
import React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import Moves from '../../../../src/components/moves/Moves';

describe('Moves Component', () => {
  test('renders with empty history', () => {
    const { container } = render(<Moves history={[]} jumpTo={() => {}} />);
    expect(container.firstChild).toBeNull();
  });

  test('renders list of moves when history has entries', () => {
    const history = [{}, {}, {}]; // Example history with 3 steps
    render(<Moves history={history} jumpTo={() => {}} />);
    const listItems = screen.getAllByRole('listitem');
    expect(listItems.length).toBe(3);
  });

  test('calls jumpTo function on button click', () => {
    const mockJumpTo = jest.fn();
    const history = [{}, {}, {}];
    render(<Moves history={history} jumpTo={mockJumpTo} />);
    const button = screen.getAllByRole('button')[0]; // First button
    fireEvent.click(button);
    expect(mockJumpTo).toHaveBeenCalledWith(0);
  });
});

