// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic-cli using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';  // Updated import for extend-expect
import GameInfo from '../../../../src/components/game-info/GameInfo';

jest.mock('../../../../src/assets/images/friends.webp', () => '');

describe('GameInfo Component', () => {
  it('should display the correct message when it is X\'s turn and no winner', () => {
    render(<GameInfo xIsNext={true} winner={null} />);
    expect(screen.getByText("It's your turn, player X")).toBeInTheDocument();
  });

  it('should display the correct message when it is O\'s turn and no winner', () => {
    render(<GameInfo xIsNext={false} winner={null} />);
    expect(screen.getByText("Now you, player O!")).toBeInTheDocument();
  });

  it('should display the winner message for X', () => {
    render(<GameInfo winner={'X'} status={'Winner: X'} />);
    expect(screen.getByText("Nice! I won!")).toBeInTheDocument();
  });

  it('should display the winner message for O', () => {
    render(<GameInfo winner={'O'} status={'Winner: O'} />);
    expect(screen.getByText("Wohoo! I made it!")).toBeInTheDocument();
  });

  it('should render the image with correct alt text', () => {
    render(<GameInfo />);
    expect(screen.getByAltText("Player X and Player O")).toBeInTheDocument();
  });
});

