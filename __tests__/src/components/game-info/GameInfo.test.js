// Test generated by RoostGPT for test reactTesting using AI Type Open AI and AI Model gpt-4-1106-preview

import React from "react";
// Import React testing utilities
import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";

// Import the component to be tested
import GameInfo from "../../../../src/components/game-info/GameInfo";

// Define the test suite
describe("GameInfo Component", () => {
  it("renders correctly", () => {
    const { getByAltText, getByText } = render(
      <GameInfo status='Playing' winner={null} xIsNext={true} />
    );

    // Check if the component renders the correct text
    expect(getByText("It's your turn, player X")).toBeInTheDocument();

    // Check if the component renders the image with the correct alt text
  });

  it("renders player O turn correctly", () => {
    const { getByText } = render(
      <GameInfo status='Playing' winner={null} xIsNext={false} />
    );

    expect(getByText("Now you, player O!")).toBeInTheDocument();
  });

  it("renders player X as winner correctly", () => {
    const { getByText } = render(
      <GameInfo status='Winner: X' winner='X' xIsNext={false} />
    );

    expect(getByText("Nice! I won!")).toBeInTheDocument();
  });

  it("renders player O as winner correctly", () => {
    const { getByText } = render(
      <GameInfo status='Winner: O' winner='O' xIsNext={false} />
    );

    expect(getByText("Wohoo! I made it!")).toBeInTheDocument();
  });
});
