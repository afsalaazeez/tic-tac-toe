// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic-cli using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
import React from 'react';
import { render, cleanup, fireEvent } from '@testing-library/react';
import Square from '../../../../src/components/square/Surface.js';

describe('Square Component Tests', () => {
    afterEach(cleanup);

    test('should render without crashing', () => {
        const { getByTestId } = render(<Square value="X" />);
        expect(getByTestId('square')).toBeInTheDocument();
    });

    test('should display value passed to "value" prop', () => {
        const { getByTestId } = render(<Square value="X" />);
        const squareElement = getByTestId('square');
        expect(squareElement.textContent).toBe('X');
    });

    test('should call onClick when the square is clicked', () => {
        const onClick = jest.fn();
        const { getByTestId } = render(<Square value="X" onClick={onClick} />);
        fireEvent.click(getByTestId('square'));
        expect(onClick).toHaveBeenCalledTimes(1);
    });

    test('should not call onClick if no onClick prop is provided', () => {
        const { getByTestId } = render(<Square value="X" />);
        fireEvent.click(getByTestId('square'));
        // No onClick prop provided, so nothing happens here and no function to expect.
    });

    // Additional test cases can include testing different values, testing error handling, etc.
});

