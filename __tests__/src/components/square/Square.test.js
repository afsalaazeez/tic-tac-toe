// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic-cli using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
import React from 'react';
import { render, fireEvent } from '@testing-library/react';
import Square from '../../../../src/components/square/Square';

describe('Square component tests', () => {
  test('should correctly render Square component with default props', () => {
    const { getByRole } = render(<Square value="X" />);
    const squareButton = getByRole('button');
    expect(squareButton).toBeInTheDocument();
    expect(squareButton.textContent).toBe('X');
  });

  test('should call onClick when the Square is clicked', () => {
    const onClickMock = jest.fn();
    const { getByRole } = render(<Square value="O" onClick={onClickMock} />);
    const squareButton = getByRole('button');
    fireEvent.click(squareButton);
    expect(onClickMock).toHaveBeenCalledTimes(1);
  });

  test('should handle edge case when value prop is undefined', () => {
    const { getByRole } = render(<Square />);
    const squareButton = getByRole('button');
    expect(squareButton.textContent).toBe('');
  });

  test('should be styled with class "square"', () => {
    const { container } = render(<Square value="X" />);
    const squareButton = container.firstChild;
    expect(squareButton).toHaveClass('square');
  });

  test('error handling when onClick is not a function', () => {
    console.error = jest.fn(); // Mock console.error
    render(<Square value="1" onClick="not-a-function" />);
    expect(console.error).toHaveBeenCalled();
  });
});

