// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic-cli using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
// Importing necessary utility for testing
import { render } from '@testing-library/react';
import reportWebVitals from '../../src/reportWebVitals';

// Defining the test suite for the reportWebVitals component
describe('reportWebVitals Component Tests', () => {
    // Test to ensure the function calls onReport without error
    test('should call onReport without errors', () => {
        const onReport = jest.fn();
        reportWebVitals(onReport);

        expect(onReport).toHaveBeenCalled();
    });

    // Test to ensure no report is generated when no function is passed
    test('should not throw error when invoked without arguments', () => {
        expect(() => reportWebVitals()).not.toThrow();
    });

    // Test to ensure that the function handles different input types
    test('should handle different input types for onReport', () => {
        const inputs = [null, undefined, 42, 'string', {}];
        inputs.forEach(input => {
            expect(() => reportWebVitals(input)).not.toThrow();
        });
    });

    // Optional: Mocking the console to check for any error messages (if the function prints any)
    test('should not log errors to the console', () => {
        const consoleSpy = jest.spyOn(console, 'error');
        reportWebVitals(jest.fn());
        
        expect(consoleSpy).not.toHaveBeenCalled();
        consoleSpy.mockRestore(); // Restore console.error to its original state
    });
});

