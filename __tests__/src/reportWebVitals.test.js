// ********RoostGPT********
// Test generated by RoostGPT for test react-tic using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
// Assuming the relative path from __tests__/src to the src directory is '../src/'
import reportWebVitals from '../../src/reportWebVitals';

describe('reportWebVitals', () => {
  let originalConsoleLog;

  beforeAll(() => {
    originalConsoleLog = console.log;
    console.log = jest.fn();
  });

  afterAll(() => {
    console.log = originalConsoleLog;
  });

  test('logs metrics when onPerfEntry function is called', () => {
    const onPerfEntry = jest.fn();
    reportWebVitals(onPerfEntry);

    // Assuming onPerfEntry would be called with a performance entry object
    expect(onPerfEntry).toHaveBeenCalledWith(expect.any(Object));
  });

  test('does not throw errors when called without params', () => {
    expect(() => reportWebVitals()).not.toThrow();
  });

  // Add additional edge cases as appropriate depending on the specific behavior of reportWebVitals.js
});

