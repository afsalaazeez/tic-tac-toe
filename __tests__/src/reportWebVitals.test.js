// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic-cli using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
import reportWebVitals from '../../src/reportWebVitals';

// Mock the 'web-vitals' module
jest.mock('web-vitals', () => ({
  getCLS: jest.fn(),
  getFID: jest.fn(),
  getFCP: jest.fn(),
  getLCP: jest.fn(),
  getTTFB: jest.fn()
}));

describe('reportWebVitals', () => {
  let mockOnPerfEntry;

  beforeEach(() => {
    mockOnPerfEntry = jest.fn();
  });

  it('should execute callback for each web vital when reportWebVitals is called with a valid function', async () => {
    await reportWebVitals(mockOnPerfEntry);
    expect(mockOnPerfEntry).toHaveBeenCalledTimes(5);
  });

  it('should not execute any callback when reportWebVitals is called without any argument', async () => {
    await reportWebVitals();
    expect(mockOnPerfEntry).not.toHaveBeenCalled();
  });

  it('should not execute any callback when the provided argument is not a function', async () => {
    await reportWebVitals("not_a_function");
    expect(mockOnPerfEntry).not.toHaveBeenCalled();
  });
});

