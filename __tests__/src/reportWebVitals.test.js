// ********RoostGPT********
// Test generated by RoostGPT for test react-tic using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
// __tests__/src/reportWebVitals.test.js

import reportWebVitals from '../../src/reportWebVitals';

describe('reportWebVitals', () => {
  beforeEach(() => {
    jest.resetModules();
  });

  it('should call onPerfEntry for each metric when onPerfEntry callback is provided', async () => {
    const mockOnPerfEntry = jest.fn();
    jest.mock('web-vitals', () => ({
      getCLS: jest.fn().mockImplementation((cb) => cb()),
      getFID: jest.fn().mockImplementation((cb) => cb()),
      getFCP: jest.fn().mockImplementation((cb) => cb()),
      getLCP: jest.fn().mockImplementation((cb) => cb()),
      getTTFB: jest.fn().mockImplementation((cb) => cb()),
    }));

    await reportWebVitals(mockOnPerfEntry);

    expect(mockOnPerfEntry).toHaveBeenCalledTimes(5);
  });

  it('should not call onPerfEntry when no callback is provided', async () => {
    const mockOnPerfEntry = jest.fn();
    await reportWebVitals(null);
    expect(mockOnPerfEntry).not.toHaveBeenCalled();
  });
});

