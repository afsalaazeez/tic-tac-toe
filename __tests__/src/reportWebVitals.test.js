// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic-cli using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
// Importing dependencies
import reportWebVitals from '../../src/reportWebVitals';

// Test suite for reportWebVitals
describe('reportWebVitals', () => {
    // Test case 1: Should correctly call reportWebVitals with a function as argument
    test('should call reportWebVitals with a mock function', () => {
        const mockOnPerfEntry = jest.fn();
        const realEntry = () => {};
        reportWebVitals(mockOnPerfEntry);
        expect(mockOnPerfEntry).toHaveBeenCalledWith(expect.any(Function));
        mockOnPerfEntry(realEntry);
    });

    // Test case 2: Should not throw error when called without arguments
    test('should not throw error when called without arguments', () => {
        expect(() => reportWebVitals()).not.toThrow();
    });

    // Adding more test cases to validate the functionality as per the requirements
    // Test case 3: Should correctly handle null as an argument
    test('should handle null appropriately', () => {
        const mockOnPerfEntry = jest.fn();
        reportWebVitals(null);
        expect(mockOnPerfEntry).not.toHaveBeenCalled();
    });

    // Test case 4: Should handle error if non-function is passed
    test('should throw error if non-function argument is passed', () => {
        const invalidArgument = 'not-a-function';
        expect(() => reportWebVitals(invalid notes)).toThrow(TypeError);
    });
});

