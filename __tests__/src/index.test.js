// ********RoostGPT********
// Test generated by RoostGPT for test react-tic using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
// Import necessary dependencies and utilities for testing
import React from 'react';
import { render, fireEvent, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import Square from '../../../components/square/Square';

// 'describe' function to create a test suite for the Square component
describe('Square component tests', () => {

  // Test case 1: Renders the Square component with the correct value.
  test('displays the correct value in the Square', () => {
    const value = 'X';
    render(<Square value={value} onClick={() => {}} />);
    const squareButton = screen.getByRole('button', { name: value });
    expect(squareButton).toHaveTextContent(value);
  });

  // Test case 2: Calls the onClick event handler when the Square is clicked.
  test('calls onClick event handler when the Square is clicked', () => {
    const handleClick = jest.fn();
    render(<Square value='X' onClick={handleClick} />);
    const squareButton = screen.getByRole('button', { name: 'X' });
    fireEvent.click(squareButton);
    expect(handleClick).toHaveBeenCalledTimes(1);
  });

});

