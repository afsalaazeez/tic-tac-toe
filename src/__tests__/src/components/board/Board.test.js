// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
import React, { useState } from 'react';

const Board = ({ size }) => {
  const [boardState, setBoardState] = useState(Array(size).fill(null));

  const handleClick = (index) => {
    setBoardState((prev) => {
      const copy = [...prev];
      copy[index] = 'X';  // Assuming a tic-tac-toe marker for simplicity.
      return copy;
    });
  };

  return (
    <div className="board">
      {boardState.map((cell, index) => (
        <button key={index} data-testid={`cell-${index}`} onClick={() => handleClick(index)}>
          {cell}
        </button>
      ))}
    </div>
  );
};

export default Board;

