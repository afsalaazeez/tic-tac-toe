// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
import React from 'react';
import { render, screen, fireEvent } from '@testing-library/react';
import Board from '../../../components/board/Board';

describe('Board Component', () => {
  const mockOnClick = jest.fn();
  const mockJumpTo = jest.fn();
  
  beforeEach(() => {
    render(<Board squares={Array(9).fill(null)} onClick={mockOnClick} jumpTo={mockJumpTo} />);
  });

  test('should render 9 squares', () => {
    const squares = screen.getAllByText('');
    expect(squares).toHaveLength(9);
  });

  test('should call onClick when a square is clicked', () => {
    const squares = screen.getAllByText('');
    fireEvent.click(squares[0]);
    expect(mockOnClick).toHaveBeenCalledWith(0);
  });

  test('should render a button to restart the game', () => {
    const button = screen.getByText('Restart the game');
    expect(button).toBeInTheDocument();
  });

  test('should call jumpTo when the restart button is clicked', () => {
    const button = screen.getByText('Restart the game');
    fireEvent.click(button);
    expect(mockJumpTo).toHaveBeenCalledWith(0);
  });
});

