// ********RoostGPT********
// Test generated by RoostGPT for test react-test-tic using AI Type Open AI and AI Model gpt-4-turbo-2024-04-09



// ********RoostGPT********
// Importing the reportWebVitals function
import reportWebVitals from '../../reportWebVitals';

// Jest test suite
describe('reportWebVitals Tests', () => {

  // Mocking the web-vitals module
  jest.mock('web-vitals', () => ({
    getCLS: jest.fn(),
    getFID: jest.fn(),
    getFCP: jest.fn(),
    getLCP: jest.fn(),
    getTTFB: jest.fn()
  }));

  beforeEach(() => {
    // Clear all mocks before each test
    jest.clearAllMocks();
  });

  test('should import web-vitals and call provided functions if onPerfEntry is a function', async () => {
    const mockOnPerfEntry = jest.fn();
    await reportWebVitals(mockOnPerfEntry);
    expect(mockOnPerfEntry).toHaveBeenCalledTimes(5); // Since web-vitals functions are 5
  });

  test('should do nothing if onPerfEntry is not provided', async () => {
    const mockOnPerfEntry = undefined;
    await reportWebVitals(mockOnPerfEntry);
    expect(mockOnPerfEntry).toBeUndefined();
  });

  test('should do nothing if onPerfEntry is not a function', async () => {
    const mockOnPerfEntry = 'not-a-function';
    await reportWebVitals(mockOnPerfEntry);
    expect(typeof mockOnPerfEntry).not.toBe('function');
  });

  // Add more tests as necessary to cover more edge cases and error handling
});

