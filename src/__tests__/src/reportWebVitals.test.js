// ********RoostGPT********
// Test generated by RoostGPT for test react-tic using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import reportWebVitals from '../src/reportWebVitals';

jest.mock('web-vitals', () => ({
  getCLS: jest.fn(),
  getFID: jest.fn(),
  getFCP: jest.fn(),
  getLCP: jest.fn(),
  getTTFB: jest.fn(),
}));

describe('reportWebVitals', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  test('should call all web vital functions when onPerfEntry is a function', async () => {
    const onPerfEntry = jest.fn();
    await reportWebVitals(onPerfEntry);
    expect(onPerfEntry).toHaveBeenCalledTimes(5);
  });

  test('should not call any web vital functions when onPerfEntry is not provided', async () => {
    await reportWebVitals();
    // It can be assumed that the web vital functions would not be called without onPerfEntry
    expect(onPerfEntry).not.toBeCalled(); // onPerfEntry is not defined in this scope, thus not callable
  });

  test('should not call any web vital functions when onPerfEntry is not a function', async () => {
    const onPerfEntry = 'not a function';
    await reportWebVitals(onPerfEntry);
    // Again, it can be assumed that the web vital functions would not be called as onPerfEntry is not a function
    expect(onPerfEntry).not.toBeCalled(); // onPerfEntry is not a function, thus not callable
  });
});

